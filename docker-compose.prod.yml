version: '3.4'
services:
  route_handler:
    build:
      dockerfile: Dockerfile
    ports:
      - '80:80'
    volumes:
      - ./php:/www/data
  client:
    build:
      dockerfile: Dockerfile
      target: prod
  php:
    build:
      dockerfile: Dockerfile
    volumes:
      - ./php:/www/data
  database:
    volumes:
      - './database:/docker-entrypoint-initdb.d'
